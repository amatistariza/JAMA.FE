# Copy this file to .env and edit values before running docker-compose
# DO NOT commit your real secrets to source control.

# Strong SA password for SQL Server
SA_PASSWORD=ChangeMeStrongP@ssw0rd

# Optionally override frontend port (host:container)
FRONTEND_PORT=4200

# Optionally override backend port (host:container)
BACKEND_PORT=5000
